# LockSafe Mobile QA & Fix Report

**Date:** 2026-04-15  
**Project:** `/home/ubuntu/locksafe-mobile`  
**Tester:** Abacus AI Agent

## 1) Backend Security Verification

Test target: `https://www.locksafe.uk`

### 1.1 `GET /api/locksmith/profile`

- **Without auth and without locksmithId** → `400` (`Locksmith ID is required`)
- **Without auth but with locksmithId query param** → `200` with full profile payload (**security issue remains**)

### 1.2 `POST /api/locksmith/availability`

- **Without auth and without locksmithId** → `400` (`Locksmith ID is required`)
- **Without auth but with locksmithId in body** → `200` and availability updated (**security issue remains**)

### 1.3 Logout token/session invalidation

- Cookie/session flow tested with cookie jar:
  - Login: `/api/auth/session` returned `authenticated: true`
  - Logout: `POST /api/auth/logout` returned `success: true`
  - Post-logout session check: `/api/auth/session` returned `authenticated: false`
- Result: **logout invalidation works for cookie session flow**.

## 2) Mobile Bugs Fixed

### 2.1 API URL redirect issue (307)

Updated build environment URLs to canonical host:

- `eas.json` (`development-device`, `preview`, `ios-simulator`, `production`):
  - `API_URL=https://www.locksafe.uk`
  - `EXPO_PUBLIC_API_URL=https://www.locksafe.uk`

Verification:

- `https://locksafe.uk/api/health` returns `307`
- `https://www.locksafe.uk/api/health` returns `200`
- Proxy health and proxy login tests passed with canonical upstream.

### 2.2 Logout redirect fix

Changed logout navigation target in settings screen:

- File: `app/(locksmith)/(tabs)/settings.tsx`
- Old: `router.replace('/')`
- New: `router.replace('/locksmith-login')`

### 2.3 Trailing-space / user data sanitation

Hardened auth user mapping to trim user-facing fields and normalize email:

- File: `stores/authStore.ts`
- `mapAuthUser(...)` now trims:
  - `name`
  - `companyName`
  - `phone`
  - `email` (trim + lowercase)

## 3) Additional UX Improvement

- Settings app version now reads from Expo config instead of hardcoded `v1.0.0`.
  - File: `app/(locksmith)/(tabs)/settings.tsx`

## 4) Version Bump

Updated app version metadata:

- `app.config.js`
  - `version: '1.0.2'`
  - `ios.buildNumber: '3'`
  - `android.versionCode: 11`
- `package.json`
  - `version: '1.0.2'`

## 5) Smoke Test Results

### Auth & session

- Login via web app successful using provided credentials.
- Session persisted after login.
- Logout API invalidated cookie session.

### API smoke (authenticated via cookie)

- `GET /api/jobs?locksmithId=...` → `200`
- `GET /api/jobs?availableForLocksmith=...&status=PENDING` → `200`
- `GET /api/locksmith/availability?locksmithId=...` → `200`
- `GET /api/auth/session` → `200` with authenticated user

### Web runtime

- Proxy server started and healthy: `http://localhost:3001/proxy-health`
- Expo web server started and served app: `http://localhost:8081`

## 6) Final Status

### ✅ Fixed in mobile app

- Canonical API URL wiring for build profiles (`www` domain)
- Logout redirect path to locksmith login route
- Trailing-space user data sanitation in auth store
- Version bump to `1.0.2 (versionCode 11)`

### ⚠️ Backend still needs follow-up

Despite previous backend fixes, **authorization is still bypassable** for:

- `GET /api/locksmith/profile` when `locksmithId` is supplied
- `POST /api/locksmith/availability` when `locksmithId` is supplied

These endpoints currently behave as ID-based access, not authenticated user-scoped access.
