<svg width="1024" height="1024" viewBox="0 0 1024 1024" xmlns="http://www.w3.org/2000/svg">
  <!-- Android Adaptive Icon Foreground -->
  <!-- Safe zone is 66% of the full icon, so elements should be centered and scaled appropriately -->

  <!-- Lock Icon (scaled for safe zone) -->
  <g transform="translate(512, 380)">
    <!-- Shackle -->
    <path d="M-72 0 A72 72 0 0 1 72 0"
          fill="none"
          stroke="white"
          stroke-width="36"
          stroke-linecap="round"/>

    <!-- Lock Body -->
    <rect x="-108" y="-8" width="216" height="180" rx="29" ry="29" fill="white"/>

    <!-- Keyhole -->
    <circle cx="0" cy="60" r="29" fill="#f97316"/>
    <path d="M-18 87 L18 87 L11 137 L-11 137 Z" fill="#f97316"/>
  </g>

  <!-- SAFE Text (scaled for safe zone) -->
  <text x="512" y="700"
        font-family="-apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif"
        font-size="82"
        font-weight="bold"
        fill="white"
        text-anchor="middle"
        dominant-baseline="middle">SAFE</text>
</svg>
